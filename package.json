{
  "name": "sculptor-workout-tracker",
  "version": "1.0.0",
  "description": "💪 Sculptor - Roman Gladiator Workout Tracking App",
  "scripts": {
    "dev": "concurrently \"npm run db:start\" \"npm run backend:dev\" \"npm run frontend:dev\"",
    "start": "npm run dev",
    "db:start": "mongod --dbpath ./data/db --bind_ip 127.0.0.1 --port 27017",
    "db:stop": "pkill -f mongod",
    "backend:dev": "cd backend && python server.py",
    "backend:install": "cd backend && pip install -r requirements.txt",
    "frontend:dev": "cd frontend && yarn start",
    "frontend:install": "cd frontend && yarn install",
    "install:all": "npm run backend:install && npm run frontend:install",
    "build": "cd frontend && yarn build",
    "test:backend": "cd backend && python -m pytest",
    "test:frontend": "cd frontend && yarn test",
    "setup": "mkdir -p data/db && npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "keywords": ["workout", "fitness", "gladiator", "roman", "tracker"],
  "author": "Sculptor Team",
  "license": "MIT"
}